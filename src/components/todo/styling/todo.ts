import styled from '@emotion/styled'

export const TodoBoxContainer = styled.div`
  display: flex;
  width: fit-content;
  overflow: hidden;
  border-radius: 20px;
  box-shadow: 0 10px 15px rgba(0, 0, 0, 0.3);
  margin: 30px auto;
`
